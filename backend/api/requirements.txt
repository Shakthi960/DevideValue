# ============================================================
# Vercel deployment — trimmed dependency set
# ============================================================
# Heavy vision/RAG packages (ultralytics, torch, opencv,
# chromadb, sentence-transformers) are excluded to stay
# within Vercel's 250 MB function size limit.  The app
# degrades gracefully when they are absent.
# ============================================================

fastapi==0.141.1
python-multipart==0.0.32

SQLAlchemy==2.0.51
psycopg2-binary==2.9.10

python-dotenv==1.1.0

pydantic==2.12.5
email-validator==2.3.0

supabase==2.28.3
storage3==2.28.3
realtime==2.28.3

google-genai==2.22.0

scikit-learn==1.7.1
pandas==2.3.3
numpy==2.1.3
joblib==1.5.1

httpx==0.28.1
